query GetCheckInByID($checkInID: ID!) {
    checkInByID(checkInID:$checkInID) {
        id
        createdAt
        completedAt
        reviewedAt
        questions {
            id
            text
            questionType
            responseType
        }
    }
}