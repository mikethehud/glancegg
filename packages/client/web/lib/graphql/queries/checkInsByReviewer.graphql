query GetCheckInsByReviewer {
    checkInsByReviewer {
        id
        createdAt
        completedAt
        reviewedAt
        expiresAt
        expired
        user {
            firstName
            lastName
        }
    }
}